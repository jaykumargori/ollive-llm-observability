apiVersion: v1
kind: ConfigMap
metadata:
  name: ollive-config
data:
  PORT: "8080"
  DATABASE_URL: "postgres://ollive:ollive@postgres:5432/ollive?sslmode=disable"
  NATS_URL: "nats://nats:4222"
  CORS_ORIGINS: "http://localhost:5173"
